import { motion } from 'motion/react';

export function TopBanner() {
  return (
    <div className="bg-green-deep text-cream-soft text-center py-3 px-4 text-sm font-semibold tracking-wide uppercase">
      Prestations mariage, EVJF & événements privés en Seine-Maritime · Devis personnalisé sous 48h
    </div>
  );
}
